// import { Token } from './token.entity';

export async function createToken(name: string, token: string, expires_in: number){
//     const tokenRepo : Repository<Token> = getRepository(Token);

//     expires_in = new Date().getTime() + (expires_in * 1000);
//     console.log(expires_in);
//     const payload = {
//         name: name,
//         token: token,
//         expires_in: expires_in
//     }
//     const savedToken : Token  = await tokenRepo.create(payload);

//     await tokenRepo.save(savedToken);


//     return savedToken;
}


export function getToken(){

}